import * as React from 'react';

import './index.scss';

const ToolBar: React.FC = () => {
  return <div className="toolbar  container">icons (heart, download, settings)</div>;
};
export default ToolBar;
